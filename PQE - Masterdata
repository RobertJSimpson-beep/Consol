let
    Source = Excel.Workbook(File.Contents(Excel.CurrentWorkbook(){0}[Name]), null, true),
    MasterData = Source{[Item="MasterData", Kind="Sheet"]}[Data],
    #"Promoted Headers" = Table.PromoteHeaders(MasterData, [PromoteAllScalars=true])
in
    #"Promoted Headers"
